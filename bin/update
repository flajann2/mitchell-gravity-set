#!/bin/bash
echo "update su"
source manifest.sh

for dir in "${!manifest[@]}"; do
    repo="${manifest[$dir]}"
    echo "*** updating $dir <== $repo"
    git subtree pull -P $dir $repo ¼branch --squash
done
